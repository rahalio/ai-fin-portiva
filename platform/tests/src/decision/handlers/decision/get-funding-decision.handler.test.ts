/**
 * GetFundingDecision handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: decision
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getFundingDecision } from "@portiva/api-server/domains/decision/handlers";
import type { DecisionDDDDependencies } from "@portiva/api-server/domains/decision";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@portiva/tests/shared";
import { DECISION_USE_CASES } from "../__factories__/use-cases.js";

describe("GetFundingDecision", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DecisionDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DecisionDDDDependencies>(
      DECISION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetFundingDecision, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { decisionId: entity.id };

    await getFundingDecision(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetFundingDecision.execute).toHaveBeenCalledWith(expect.objectContaining({ decisionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { decisionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetFundingDecision, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getFundingDecision(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
