/**
 * UpdateInitiative handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: initiative
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateInitiative } from "@portiva/api-server/domains/initiative/handlers";
import type { InitiativeDDDDependencies } from "@portiva/api-server/domains/initiative";
import { createInitiative as createInitiativeEntity } from "@portiva/factories/initiative";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@portiva/tests/shared";
import { INITIATIVE_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateInitiative", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: InitiativeDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<InitiativeDDDDependencies>(
      INITIATIVE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createInitiativeEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { initiativeId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateInitiative, "execute").mockResolvedValue(useCaseResult);

    await updateInitiative(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateInitiative.execute).toHaveBeenCalledWith(expect.objectContaining({ initiativeId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
