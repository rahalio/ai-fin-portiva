/**
 * Initiative Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: initiative
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { InitiativeEntity } from "@portiva/core/initiative/models/index.js";
import { initiativeSchemas } from "@portiva/core/initiative";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "ini";

export function createInitiative(overrides: Partial<InitiativeEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    arcs: overrides.arcs ?? [],
    economicBuyer: overrides.economicBuyer ?? faker.lorem.word(),
    initiativeId: overrides.initiativeId ?? `ini_${ulid().toLowerCase()}`,
    name: overrides.name ?? faker.person.fullName(),
    ownerUserId: overrides.ownerUserId ?? `usr_${ulid().toLowerCase()}`,
    stage: overrides.stage ?? "explore",
    status: overrides.status ?? "active",
    ...overrides,
  };

  return initiativeSchemas.Initiative.parse(raw);
}

export function initiativeBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createInitiative(overrides));
}
