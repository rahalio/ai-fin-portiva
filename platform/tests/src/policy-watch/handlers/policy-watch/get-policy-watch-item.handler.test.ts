/**
 * GetPolicyWatchItem handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: policy-watch
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getPolicyWatchItem } from "@portiva/api-server/domains/policy-watch/handlers";
import type { PolicyWatchDDDDependencies } from "@portiva/api-server/domains/policy-watch";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@portiva/tests/shared";
import { POLICYWATCH_USE_CASES } from "../__factories__/use-cases.js";

describe("GetPolicyWatchItem", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PolicyWatchDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PolicyWatchDDDDependencies>(
      POLICYWATCH_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetPolicyWatchItem, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { policyWatchId: entity.id };

    await getPolicyWatchItem(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetPolicyWatchItem.execute).toHaveBeenCalledWith(expect.objectContaining({ policyWatchId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { policyWatchId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetPolicyWatchItem, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getPolicyWatchItem(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
