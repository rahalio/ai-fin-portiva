/**
 * ExecuteGetVendorDependency - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dependency
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetVendorDependency Use Case
 *
 * DDD: Application use case for get Dependency.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetVendorDependencyInput, GetVendorDependencyOutput } from "../dto/dependency.dto";
import type { ExecutionContextService, IdGeneratorService } from "@portiva/services/_shared/index.js";
import type { DependencyRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetVendorDependency {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly dependency: DependencyRepository
  ) {}

  async execute(input: GetVendorDependencyInput): Promise<GetVendorDependencyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.depId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).dependencyId) {
      throw new ValidationError("dependencyId is required");
    }
    const dependencyId = (input as any).dependencyId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.dependency.getVendorDependency({ dependencyId: dependencyId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${dependencyId}`);
    }

    return entity as GetVendorDependencyOutput;
  }
}
