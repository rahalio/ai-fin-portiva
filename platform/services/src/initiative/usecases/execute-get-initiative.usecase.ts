/**
 * ExecuteGetInitiative - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: initiative
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetInitiative Use Case
 *
 * DDD: Application use case for get Initiative.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetInitiativeInput, GetInitiativeOutput } from "../dto/initiative.dto";
import type { ExecutionContextService, IdGeneratorService } from "@portiva/services/_shared/index.js";
import type { InitiativeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetInitiative {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly initiative: InitiativeRepository
  ) {}

  async execute(input: GetInitiativeInput): Promise<GetInitiativeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.iniId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).initiativeId) {
      throw new ValidationError("initiativeId is required");
    }
    const initiativeId = (input as any).initiativeId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.initiative.getInitiative({ initiativeId: initiativeId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${initiativeId}`);
    }

    return entity as GetInitiativeOutput;
  }
}
