/**
 * PolicyWatch View — codegen stub.
 * Product UI lives at handwritten routes in App.tsx / pages.tsx.
 */

import { Link } from 'react-router-dom';
import { PolicyWatchViewProps } from './types';

export function PolicyWatchView({}: PolicyWatchViewProps) {
  return (
    <div className="stub-note">
      <h1>Policy watch (generated stub)</h1>
      <p>
        Use the product console: <Link to="/policy">Policy watch</Link>.
      </p>
    </div>
  );
}
