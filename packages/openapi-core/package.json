{
  "name": "@portiva/openapi-core",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI contracts for Portiva (common + identity + product domains)",
  "scripts": {
    "lint:domains": "redocly lint",
    "bundle:domains": "node ../../scripts/bundle-openapi.mjs",
    "lint": "pnpm run lint:domains",
    "bundle": "pnpm run bundle:domains"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.0"
  },
  "engines": {
    "node": ">=18"
  }
}
